#
# This is the configuration file for Rootkit Hunter.
#
# Please modify it to your own requirements.
#
# Lines beginning with a hash (#), and blank lines, will be ignored.
#

#
# Do not change this.
#
LATESTVERSION=/rkhunter_latest.dat

#
# Email a message to this address when a warning is found.
# Multiple addresses may be specified simply be separating them
# with a space.
#
#MAIL-ON-WARNING=me@mydomain   root@mydomain

#
# Specify the mail command to use if MAIL-ON-WARNING is set.
# NOTE: Double quotes are not required around the command, but
# are required around the subject line if it contains spaces.
#
MAIL_CMD=mail -s "[rkhunter] Warnings found for ${HOST_NAME}"

#
# Specify the temporary directory to use.
# NOTE: Do not use /tmp as your temporary directory. Some
# important files will be written to this directory. Be sure
# that the directory permissions are tight.
#
#TMPDIR=/usr/local/var/lib/rkhunter/tmp

#
# Specify the database directory to use.
#
#DBDIR=/usr/local/var/lib/rkhunter

#
# Specify the script directory to use.
#
#SCRIPTDIR=/usr/local/lib/rkhunter/scripts

#
# Specify the root directory to use.
#
#ROOTDIR=""

#
# Specify the command directories to be checked. This is a
# space-separated list of directories.
#
#BINDIR="/bin /usr/bin /sbin /usr/sbin /usr/local/bin /usr/local/sbin /usr/libexec /usr/local/libexec"

#
# Specify the language to use. This must be the ISO 639 language code.
#
#LANGUAGE=en

#
# Specify the log file pathname.
#
LOGFILE=/var/log/rkhunter.log

#
# Set the following option to 1 if the log file is to be appended to
# whenever rkhunter is run.
#
APPEND_LOG=0

#
# Set the following option to 1 if the second colour set is to be used.
# This can be useful if your screen uses black characters on a white
# background (e.g. a PC instead of a server).
#
COLOR_SET2=0

#
# Set the following option to 0 if RKH should not detect if X is being
# used. If X is detected as being used, then the second colour set will
# automatically be used.
#
AUTO_X_DETECT=1

#
# Set the following option to 1 if root is allowed to login via SSH.
# Do not modify this option unless you understand the warning message
# displayed by RKH about this option.
#
ALLOW_SSH_ROOT_USER=0

#
# These two variables determine which tests are to be performed.
# The ENABLE_TESTS can use the word 'all' to refer to all the
# available tests. The DISABLE_TESTS can use the word 'none' to
# mean that no tests are disabled. Both variables are a 
# space-separated list of test names. The current names can be
# seen by using 'rkhunter --list tests'. The program default is
# to enable all tests, and disable no tests. The list of disabled
# tests is applied to the list of enabled tests.
#
ENABLE_TESTS="all"
DISABLE_TESTS="known_bad_hash deleted_files packet_cap_apps"

#
# The HASH_FUNC option can be used to specify the command to use for
# hash checking. It can be specified as just the command name or the
# full pathname. Systems using prelinking are restricted to using
# either SHA1 or MD5 functions. To get RKH to look for the sha1(sum)/md5(sum)
# command, or to use the supplied perl scripts, simply specify this
# option as 'SHA1' or 'MD5' in uppercase.
# The default in all cases is SHA1.
#
# Examples:
#   For Solaris 9 : HASH_FUNC=gmd5sum
#   For Solaris 10: HASH_FUNC=sha1sum
#   For AIX (>5.2): HASH_FUNC="csum -hMD5"
#   For NetBSD    : HASH_FUNC="cksum -n -a sha512"
#
# NOTE: If the hash function is changed then you MUST run RKH with
#       the '--hashupd' option to rebuild the hash database.
#
#HASH_FUNC=sha1sum

#
# To perform the 'known bad' MD5 hash file check requires a
# valid MD5 hash function command. This may automatically be
# determined via the HASH_FUNC above, alternatively the user
# can specify a different command to use here. By default the
# command 'md5sum', and then 'md5', will be looked for. If they
# are not found, then the supplied perl script will be used if
# possible.
#
#MD5_CMD=md5sum

#
# Whitelist the specified files and their hash value. The hash
# function used is specified by HASH_FUNC above.
#
# Usage: HASHWHITELIST=<file name>:<hash value>
#
#HASHWHITELIST=ps:9bd8bf260adc81d3a43a086fce6b430a
#HASHWHITELIST=ps:404583a6b166c2f7ac1287445a9de6b3

#
# Allow the specified hidden directories.
# One directory per line (use multiple ALLOWHIDDENDIR lines).
#
#ALLOWHIDDENDIR=/etc/.java
#ALLOWHIDDENDIR=/dev/.udev
#ALLOWHIDDENDIR=/dev/.udevdb
#ALLOWHIDDENDIR=/dev/.udev.tdb
#ALLOWHIDDENDIR=/dev/.static
#ALLOWHIDDENDIR=/dev/.initramfs
#ALLOWHIDDENDIR=/dev/.SRC-unix

#
# Allow the specified hidden files.
# One file per line (use multiple ALLOWHIDDENFILE lines).
# 
#ALLOWHIDDENFILE=/etc/.java
#ALLOWHIDDENFILE=/usr/share/man/man1/..1.gz
#ALLOWHIDDENFILE=/etc/.pwd.lock
#ALLOWHIDDENFILE=/etc/.init.state

#
# Allow the specified processes to use deleted files.
# One process per line (use multiple ALLOWPROCDELFILE lines).
#
#ALLOWPROCDELFILE=/sbin/cardmgr
#ALLOWPROCDELFILE=/usr/sbin/gpm
#ALLOWPROCDELFILE=/usr/libexec/gconfd-2
#ALLOWPROCDELFILE=/usr/sbin/mysqld

#
# Allow the specified processes to listen on any network interface.
# One process per line (use multiple ALLOWPROCLISTEN lines).
#
#ALLOWPROCLISTEN=/sbin/dhclient
#ALLOWPROCLISTEN=/usr/bin/dhcpcd
#ALLOWPROCLISTEN=/usr/sbin/pppoe
#ALLOWPROCLISTEN=/usr/sbin/tcpdump
#ALLOWPROCLISTEN=/usr/sbin/snort-plain
#ALLOWPROCLISTEN=/usr/local/bin/wpa_supplicant

